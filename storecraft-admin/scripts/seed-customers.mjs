/**
 * DISABLED for launch — this script previously seeded demo/template catalog data
 * (piercing jewelry / demo customers / sample blogs) and must not run against production.
 *
 * Original file preserved in git history only.
 * Use Admin UI or Shopify to manage live catalog content.
 */
console.error("storecraft-admin/scripts/seed-customers.mjs is disabled. Do not run demo seed scripts against production.");
process.exit(1);
